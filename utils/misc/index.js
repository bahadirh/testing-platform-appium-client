const clearElement = require('./clear-element')
const getWindowSize = require('./get-window-size')
const hideKeyboard = require('./hide-keyboard')
const lockDevice = require('./lock')
const resetApp = require('./reset-app')
const shake = require('./shake')
const unlockDevice = require('./unlock')

module.exports = {
  clearElement,
  getWindowSize,
  hideKeyboard,
  lockDevice,
  resetApp,
  shake,
  unlockDevice
}
